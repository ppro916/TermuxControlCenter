buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // AGP version stable for GitHub Actions
        classpath "com.android.tools.build:gradle:8.0.2"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
